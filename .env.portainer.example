# Used by Portainer stack env (Repository deploy).
# Do not commit real secrets.

DISCORD_TOKEN=
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-4-6

# Absolute path on the Docker host (rclone sync target)
HOST_LOG_DIR=/mnt/robot-logs/2026-Rebuilt

# ALLOWED_CHANNEL_IDS=
# ALLOWED_USER_IDS=
